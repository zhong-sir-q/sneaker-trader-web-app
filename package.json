{
  "name": "main",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "buildAll": "tsc -b frontend backend",
    "buildBackend": "tsc -b backend",
    "start": "node --prefix backend build/src/start.js",
    "postinstall": "npm run installBackend && npm run buildBackend",
    "dev": "concurrently 'npm --prefix frontend start' 'npm --prefix backend start'",
    "deployAll": "./build_and_deploy.sh && npm --prefix frontend run build && npm --prefix frontend run deploy",
    "push": "npm --prefix backend test && git push && npm run deployAll",
    "installBackend": "npm --prefix backend install"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^5.3.0",
    "typescript": "^4.0.2"
  },
  "dependencies": {}
}
