version: '3.0'
services: 
    backend:
        build: .
        ports: 
            - "4000:4000"
        env_file: 
            - ./backend/.env.production
        environment:
            - NODE_ENV=production
